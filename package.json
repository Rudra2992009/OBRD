{
  "name": "obrd-hybrid-detection",
  "version": "2.0.0",
  "description": "Hybrid offline+cloud object detection PWA",
  "scripts": {
    "start": "npx http-server -p 8080",
    "api": "node api-server/server.js"
  }
}
